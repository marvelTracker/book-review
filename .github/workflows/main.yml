on:
  push:
    branches:
    - main
    - release/*
  pull_request:
    branches:
    - main
  workflow_dispatch:

jobs:
  my_job:
    name: build
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v4

    - name: Run hello-world
      run: echo Hello, world !!
  
